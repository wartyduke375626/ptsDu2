package integrationTests;

public class DBPathsAndURLs {

    //change this string to match the path of testDB.db relative to project root directory
    public static final String TEST_DB_PATH = "testDB.db";

    public static final String TEST_DB_URL = "jdbc:sqlite:" + TEST_DB_PATH;
}
